# repomap documentation

- [Installation](installation.md) — Homebrew, `go install`, Linux packages, and verifying signed archives.
- [Quick start](quickstart.md) — the fastest path from zero to useful output.
- [Command reference](commands.md) — every command, flag, and exit code.
- [Architecture](architecture.md) — how the CLI, library, loader, and detectors fit together.
- [Schema reference](schema.md) — the `repomap.v1` JSON contract.

For agent integration snippets (Cursor rules and `AGENTS.md`), see
[`examples/`](../examples).
